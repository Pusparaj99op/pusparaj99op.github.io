/* Mouse trail removed for performance reasons. */
/* left as a stub to prevent reference errors if other scripts try to import this file. */
export default function noopMouseTrail() {
    return null;
}
